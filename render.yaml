services:
  - type: web
    name: youtube-analyzer
    runtime: docker
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: FLASK_DEBUG
        value: "0"
      - key: PORT
        value: "8080"
